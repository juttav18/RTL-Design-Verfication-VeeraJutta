module Down_counter(input clk,rst,
                  output reg count);
    
    reg [7:0]temp;
    
    always@(posedge clk)begin
        if (rst) 
            temp <= 8'b0;
        else 
            temp <= temp - 1;
    end
    
    assign count = temp;
    
endmodule
